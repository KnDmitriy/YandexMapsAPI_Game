{
  "name": "guessing-game-with-yandex-maps",
  "version": "1.0.0",
  "description": "",
  "main": "script.js",
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && echo 'Ready to serve'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "firebase": "^10.12.1",
    "firebaseui": "^6.1.0"
  },
  "devDependencies": {
    "dotenv": "^16.0.0"
  }
}
